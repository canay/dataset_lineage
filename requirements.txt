# Primary analysis environment (Adult, ACSIncome, German Credit, and audit statistics).
# Python 3.10.12 (see results/versions.txt). Pin these for exact reproduction of the
# main tables; the Heart Disease extension used a separate stack (requirements-heart.txt).
scikit-learn==1.7.2
pandas==2.3.3
numpy==2.2.6
scipy==1.15.3
matplotlib==3.10.9
